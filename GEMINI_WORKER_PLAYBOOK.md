# 보조 작업자 플레이북

목표: 장문 리포트형 작업자가 아니라, 짧고 검증 가능한 보조 작업으로만 사용한다.

## 0) 현재 사용할 보조 작업자 채널
- 사용 채널: Codex thread
- 대화 제목: `보조작업자 지정`
- thread id: `019ecfc6-a204-7960-8c19-2a87fc03f6c4`
- 작업 폴더: `C:\Users\qpalz\Documents\remote`
- 비고: 기존 Gemini/Antigravity 대화는 단일 산출물 작업도 지키지 못했으므로 기본 보조 작업자로 쓰지 않는다.

## 1) Gemini에게 맡길 수 있는 작업
- 특정 파일에서 한 줄 검색  
  - 조건: 키워드/패턴 1개, 경로 1개, 한 줄만 반환
- 한 파일 diff 요약  
  - 대상: 단일 파일 변경분만 3~5줄 이하로 요약
- 지정 테스트 1개 마지막 20줄 출력  
- 단순 오탈자 후보 나열  
  - 가능하면 라인 번호/문자열 제안만 간단히

## 2) Gemini에게 맡기면 안 되는 작업
- 전체 테스트 수 최종 보고
- 벤치마크 해석
- 릴리스 가능 여부 판단
- 전체 프로젝트 완료율 계산
- 여러 파일을 묶은 종합 결론/판단

## 3) 요청 템플릿(반드시 3줄 응답 형식으로 요청)

### 템플릿 A: 파일 검색
1. `요청:` `한 파일에서 "<패턴>" 검색`  
2. `범위:` `<파일 경로>`  
3. `출력:` `파일명:줄번호:내용` 형식 1줄

### 템플릿 B: 단일 테스트 실행
1. `요청:` `단일 테스트 "<테스트명>" 실행`  
2. `명령:` `<실행 명령>`  
3. `출력:` 마지막 20줄만 1회 출력

### 템플릿 C: 단일 파일 diff 확인
1. `요청:` `단일 파일 diff 확인`  
2. `범위:` `<파일 경로>`  
3. `출력:` 추가/삭제/변경 라인만 1블록 요약

## 4) 실패 시 처리 규칙
- 응답 형식(3줄/항목 규칙) 위반 시 폐기
- 요청한 키워드 또는 문자열 누락 시 폐기
- 금지어(예: 전체 완료율, 전체 테스트, 릴리스 판단, 종합 결론) 사용 시 폐기
- 위반 결과는 다시 채택하지 않으며 재시도 요청도 금지

## 5) CodeX 원칙
- Gemini 응답 도착까지 대기하고 그대로 반영하지 않는다.
- 응답을 채택하기 전, 로컬에서 동일 범위로 재검증한다.
- 재검증 결과가 다르면 해당 응답은 폐기하고 로컬 기준으로 재작업한다.
