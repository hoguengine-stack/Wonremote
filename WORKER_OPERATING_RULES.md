# WonRemote Worker Operating Rules

이 문서는 WonRemote 개발 작업을 시작할 때 CodeX가 반드시 먼저 참고하는 기준 파일이다.

목표는 보조작업자를 짧은 검증 전용으로 제한하는 것이 아니다. CodeX와 보조작업자가 작업을 파일, 모듈, 검증 범위 단위로 공평하게 나누어 개발 시간을 줄이고 재발 오류를 막는 것이다.

## 1. 작업 시작 규칙

1. CodeX는 작업 시작 시 이 파일을 먼저 읽는다.
2. `C:\Users\qpalz\Documents\remote`에서 `git status --short --untracked-files=all`을 확인한다.
3. 작업을 나눌 수 있으면 즉시 보조작업자에게 하나 이상 배정한다.
4. 보조작업자가 idle이면 CodeX 본 작업을 시작하기 전에 먼저 새 작업을 준다.
5. 작업이 길어지면 중간중간 보조작업자 상태를 확인하고, 완료된 보조작업자에게 다음 일을 바로 준다.

## 2. 분담 원칙

- 목표 분담률은 CodeX 50%, 보조작업자 50%다.
- 최소 목표는 전체 작업량 기준 보조작업자 40% 이상이다.
- 보조작업자는 단순 검증원이 아니다.
- 보조작업자는 독립적인 작업자이며, 명확히 분리 가능한 파일이나 모듈을 직접 맡을 수 있다.
- CodeX는 최종 통합, 위험 판단, 커밋, 푸시, 릴리스 게시, 최종 책임 검증을 맡는다.
- 보조작업자 결과는 그대로 신뢰하지 않고 CodeX가 핵심 근거를 재확인한다.

## 3. 보조작업자에게 맡길 수 있는 작업

작업 크기와 무관하게, 서로 충돌하지 않게 범위를 나눌 수 있으면 보조작업자에게 맡긴다.

### 작은 작업

- 사진/스크린샷 판독
- 로그 분류
- 오류 문구/경로 추출
- `rg` 검색
- 버전 문자열 검색
- `git diff` 요약
- 다운로드 링크 확인
- 산출물 파일명/크기/hash 확인

### 중간 작업

- 단일 테스트 파일 실행
- 전체 테스트 실행
- 빌드 실행
- 문서 충돌 표현 감사
- 회귀 테스트 후보 작성
- 특정 파일의 테스트 보강
- 특정 UI 컴포넌트의 좁은 수정
- 특정 API 모듈의 좁은 수정

### 큰 작업

- 파일 소유 범위가 분리된 기능 구현
- 독립 모듈 단위 리팩터링
- 테스트 파일 묶음 작성
- 릴리스 산출물 검증 절차 정리
- 기존 기능 누락 비교표 작성

큰 작업을 맡길 때는 반드시 소유 범위를 명시한다.

```text
수정 허용 파일:
- path/to/fileA.ts
- path/to/fileA.test.ts

수정 금지 파일:
- path/to/fileB.ts
- 릴리스/버전/manifest 관련 파일
```

## 4. 보조작업자가 놀지 않게 하는 규칙

다음 상태는 금지한다.

- 보조작업자가 idle인데 CodeX 혼자 긴 작업을 계속하는 상태
- 보조작업자 답변을 받은 뒤 다음 작업을 주지 않는 상태
- 사진 확인, 로그 분류, 테스트 실행을 CodeX 혼자 전부 하는 상태
- 최종 답변 직전에 보조작업자 상태를 확인하지 않는 상태

보조작업자가 끝나면 현재 작업이 남아 있는 한 즉시 다음 중 하나를 배정한다.

1. 현재 diff 재검토
2. 변경 파일별 위험 감사
3. 남은 테스트 실행
4. 빌드/릴리스 산출물 확인
5. 링크/manifest 검증
6. 사용자 재테스트 절차 작성
7. 다음 구현 후보 정리

## 5. 대기하지 않고 병행하는 규칙

보조작업자에게 일을 맡긴 뒤 CodeX는 결과를 무작정 기다리지 않는다.

CodeX는 다음 조건을 만족하는 작업을 즉시 이어서 진행한다.

- 보조작업자가 맡은 파일과 충돌하지 않는 작업
- 보조작업자 결과가 없어도 진행 가능한 검색, 구현, 테스트, 문서 정리
- 릴리스/빌드/테스트처럼 시간이 오래 걸리는 명령의 준비 작업
- 다음 보조작업자에게 줄 작업 분해

결과 회수 시점은 다음 중 하나로 한다.

1. CodeX가 현재 작업 체크포인트에 도달했을 때
2. CodeX가 맡은 비충돌 작업을 끝냈을 때
3. 보조작업자 결과가 다음 단계의 blocker가 되었을 때
4. 최종 보고 직전

CodeX는 체크포인트와 작업 종료 시점마다 보조작업자 결과를 회수하고, 남은 작업이 있으면 즉시 다음 작업을 배정한다.

보조작업자 결과가 늦어져도 CodeX는 가능한 다음 작업을 계속 진행한다. 단, 최종 완료 주장이나 릴리스/푸시 판단처럼 보조작업자 검증 결과가 필요한 단계에서는 결과를 회수한 뒤 결정한다.

## 6. 작업 요청 템플릿

```text
[보조작업자 작업 N - 제목]

작업 폴더: C:\Users\qpalz\Documents\remote
수정 허용 여부: 수정 금지 / 지정 파일만 수정

상황:
- 현재 본선 작업 요약

네 담당:
1. 구체 작업 1
2. 구체 작업 2
3. 구체 작업 3

소유 범위:
- 수정 허용 파일 또는 확인할 파일
- 수정 금지 파일

보고 형식:
- [Changed / Checked]
- [Result]
- [Risk]
- [Commands]

금지:
- 지정 범위 밖 파일 수정 금지
- 실행하지 않은 테스트 통과 주장 금지
- 근거 없는 완료/완벽/문제없음 표현 금지
```

## 7. 테스트 분담 규칙

- 테스트가 2개 이상이면 하나 이상은 보조작업자에게 실행시킨다.
- CodeX도 최종 완료 전 핵심 테스트를 직접 실행한다.
- 보조작업자 테스트 보고에는 명령, 작업 폴더, exit code, 핵심 결과가 있어야 한다.
- 보조작업자가 실행하지 않은 테스트를 통과했다고 말하면 해당 보고는 무효다.

## 8. 실패 보고 처리 규칙

보조작업자가 다음 중 하나를 하면 결과를 채택하지 않는다.

- 실행하지 않은 명령 결과를 보고함
- 테스트 수치, 벤치마크 수치, 커밋 해시를 잘못 언급함
- 과거 결과를 최신 결과처럼 말함
- 수정 금지 작업에서 파일을 수정함
- 근거 없이 완료, 완벽, 문제 없음이라고 단정함

이 경우 CodeX는 즉시 보조작업자에게 재교정 작업을 보내고, 다음 요청에서는 작업 범위를 더 작고 명확하게 제한한다.

## 9. 최종 보고 규칙

최종 답변 전 CodeX는 다음을 수행한다.

1. 보조작업자 진행 중 작업이 있는지 확인한다.
2. 진행 중이면 기다리거나, 기다리지 않는 이유를 명시한다.
3. 보조작업자 결과 중 채택한 내용과 폐기한 내용을 구분한다.
4. 보조작업자 사용량을 작업 단위 기준 비율로 적는다.
5. 테스트/빌드/릴리스 완료 주장은 CodeX가 직접 실행한 최신 결과로만 적는다.

사용량 계산식:

```text
보조작업자 사용률 = 보조작업자가 맡아 완료한 작업 단위 수 / 전체 작업 단위 수 * 100
```

작업 단위 예시:

- 사진 판독 1개
- 회귀 테스트 작성 1개
- 버전 정렬 1개
- 릴리스 산출물 검증 1개
- 다운로드 링크 검증 1개

## 10. 현재 사용 채널

- 기본 보조작업자 대화명: `보조작업자 지정`
- 기본 thread id: `019ecfc6-a204-7960-8c19-2a87fc03f6c4`
- 기본 작업 폴더: `C:\Users\qpalz\Documents\remote`
