# 저지연 원격접속 프로그램 개발 계획서

개정일: 2026-06-09

## 1. 결론

이 프로젝트는 단순한 `ZOOKAgent`/`ZOOKViewer` 대체 프로그램 제작이 아니다. 실제 난이도는 원격 데스크톱, 저지연 영상 스트리밍, Windows 권한 제어, 보안 제품, 업데이트 시스템을 동시에 만드는 수준이다.

가장 높은 결과물을 얻으려면 처음부터 "완벽히 같은 기능"을 목표로 잡으면 안 된다. 먼저 기존 제품보다 확실히 낮은 지연을 내는 핵심 원격 화면 제어 엔진을 검증하고, 그 다음 기능을 붙여야 한다.

권장 전략:

1. 기존 제품은 기능 조사와 성능 비교 대상으로만 사용한다.
2. 코드는 새로 작성하고, UI/이미지/프로토콜/바이너리는 복제하지 않는다.
3. 원격으로 제어될 Agent PC는 `J1800/J1900 + RAM 2GB`를 최저 기준으로 잡는다.
4. 첫 6주는 제품 개발이 아니라 저사양 PC 성능 검증 PoC에 사용한다.
5. PoC가 기존 제품보다 명확히 빠르지 않으면 전체 개발을 중단하거나 방향을 바꾼다.
6. UAC, 잠금화면, 무인 접속, 파일 전송, 녹화는 MVP 이후로 미룬다.

## 2. 현실적인 목표 재정의

### 잘못된 목표

- 지연이 없는 원격접속
- 기존 제품과 완벽히 같은 기능을 단기간에 구현
- 모든 네트워크 환경에서 빠른 원격제어
- 보안 경고 없이 관리자 권한 화면까지 완전 제어

### 올바른 목표

- 같은 환경에서 기존 제품보다 체감 지연이 낮은 원격제어
- 네트워크 지연을 없애는 것이 아니라 캡처, 인코딩, 전송, 디코딩, 렌더링 지연을 최소화
- 핵심 기능부터 검증하고, 고위험 기능은 단계적으로 추가
- 보안 제품으로 악용되지 않도록 인증, 동의, 로그, 권한 분리를 기본 설계에 포함

## 3. 기존 프로그램에서 추정되는 기능 범위

확인한 설치 경로:

- `C:\Program Files (x86)\Mastersoft\ZOOKAgent`
- `C:\Program Files (x86)\Mastersoft\ZOOKViewer`

파일 구성상 다음 기능이 포함된 것으로 추정된다.

- Agent와 Viewer로 분리된 구조
- 화면 캡처 및 원격 화면 전송
- 키보드/마우스 입력 전달
- 채팅
- 오디오 또는 음성 관련 기능
- 화면 녹화
- 자동 업데이트
- Windows 서비스 또는 런처 기반 실행
- Alt-Tab, 보안 데스크톱, 드라이버 기반 제어 가능성

주의: 이 목록은 파일명 기반 추정이다. 기능 명세로 확정하려면 실제 사용 테스트가 필요하다.

## 4. 비관적 전제

계획은 다음 전제를 기준으로 잡는다.

- 모든 사용자가 최신 GPU를 쓰지 않는다.
- 기업망에서는 UDP가 막히거나 TURN Relay로 떨어질 수 있다.
- WebRTC를 붙였다고 자동으로 빠른 제품이 되지 않는다.
- Windows 캡처 API는 보안 화면, DRM, 관리자 권한 창에서 제약이 있다.
- 백신, EDR, 방화벽이 원격제어 프로그램을 의심할 수 있다.
- 코드 서명 없이는 설치, 업데이트, 권한 상승 UX가 크게 나빠진다.
- "기존 제품과 동일 기능"은 법적, 기술적, 일정상 매우 위험한 표현이다.
- 실사용 품질은 평균 지연이 아니라 p95/p99 지연, 끊김, 재연결, 화질 저하로 결정된다.
- 원격 제어 대상 PC의 상당수가 `J1800/J1900 + RAM 2GB`라면, 1080p 30fps 실시간 인코딩은 기본 목표로 잡으면 안 된다.
- 2GB RAM 장비에서는 Electron/Tauri 같은 무거운 Agent UI가 제품 전체를 망칠 수 있다.
- `1080x1920`은 일반 FHD 가로 화면보다 픽셀 수가 같더라도 세로형 UI, 스크롤, 회전 처리 때문에 별도 최악 조건으로 테스트해야 한다.

## 5. 성공 기준

### 5.1 PoC 성공 기준 및 측정 방법

PoC는 다음 조건을 만족해야 다음 단계로 간다.

#### 5.1-A 우회 불가능한 필수 합격 조건 (지연 시간 개선)
- **기존 ZOOK 대비 p95 및 p99 E2E 입력-화면반영 지연의 수치적 개선 증명이 필수 조건이다.**
- 지연 시간이 개선되지 않으면 끊김, CPU 사용률, 메모리 사용량이 좋아져도 PoC는 실패로 판정한다.
- 지연 개선 판정은 동일 장비, 동일 해상도, 동일 네트워크 조건, 동일 테스트 시나리오에서만 인정한다.
- **해상도별 LAN p95 E2E 지연 한도:**
  - `1024x768` 기준 LAN p95 지연 **90ms 이하**
  - `1600x900` 기준 LAN p95 지연 **120ms 이하**
  - `1080x1920` 기준 LAN p95 지연 **150ms 이하**
- **외부 인터넷 환경 (RTT 30ms~60ms) p95 E2E 지연 180ms 이하**

#### 5.1-B 측정 방식 정의 (Visual Ping/Flash Telemetry Test)
- 지연은 프로그램 내 **정밀 자동 원격 루프백 계측(Visual Ping)** 방식으로 측정한다.
  1. **T_start**: Viewer가 색상 변경 입력 이벤트(Red -> Green -> Blue)를 전송하는 순간 고해상도 타임스탬프를 기록한다.
  2. **T_recv / T_inject**: Agent가 이벤트를 수신한 뒤 Windows API `SendInput`으로 테스트 윈도우에 입력을 주입한다.
  3. **T_change**: Agent의 테스트 윈도우가 입력을 받아 배경색을 변경한다.
  4. **T_cap**: Agent가 변경된 색상의 화면 프레임을 DXGI 경로로 캡처하고 인코딩 후 전송한다.
  5. **T_decode_detect**: Viewer가 수신된 비디오 프레임 버퍼를 디코딩하여 지정 픽셀 영역의 색상 변화를 최초 검출하는 순간 타임스탬프를 기록한다.
  6. **T_presented**: Viewer가 해당 프레임을 Direct3D/WebGL 등 렌더링 경로에 올리고 SwapChain `Present` 및 GPU Fence/Flush 완료를 확인한 순간 타임스탬프를 기록한다.
  7. **E2E Latency = T_presented - T_start**로 정의한다.
- `T_decode_detect`는 디코딩 완료 지표로만 사용하고, PoC 합격 판정은 최종 화면 표시 지연을 포함하는 `T_presented` 기준으로 한다.
- 측정값은 최소 1ms 단위로 기록하고 p50, p95, p99, 최대값을 산출한다.
- 소프트웨어 계측 편향을 확인하기 위해 PoC 종료 전 최소 1회는 고속 카메라 또는 외부 촬영 방식으로 교차 검증한다.

#### 5.1-C 일반 성능/품질 조건 (아래 항목 중 1개 이상 우위 필수)
- 화면 정지 상태에서 불필요한 비트레이트가 낮을 것.
- 빠른 스크롤/드래그 중에도 입력 누락이 없을 것.
- J1800/J1900 Agent CPU 사용률이 일반 화면 조작 중 지속적으로 40%를 넘지 않을 것.
- Agent 메모리 사용량이 평상시 150MB 이하, 순간 피크 250MB 이하일 것.
- `1080x1920`에서는 30fps가 아니라 조작 가능성을 우선하고, 필요하면 모션 중 자동 축소 전송을 허용할 것.
- GPU 인코딩이 불가능한 PC에서도 기능 저하 모드로 동작할 것.
- 기존 ZOOK 대비 끊김, CPU 사용률, 메모리 사용량 중 최소 1개 항목에서 추가 우위가 확인될 것.

### 5.2 MVP 성공 기준

- 접속 코드 기반으로 Agent와 Viewer 연결
- 원격 화면 보기
- 마우스/키보드 제어
- 다중 모니터 중 하나 선택
- 품질 자동 조절
- 세션 종료
- 기본 감사 로그
- 설치형 Agent 실행
- Viewer 단독 실행

### 5.3 최종 원격 장비 관리 및 접속 기능 요구사항 (최종 목표)

PoC 및 MVP 단계를 거쳐 상용 운영 수준에서 구현될 최종 원격 제어 관리 기능 명세입니다.

1. **Viewer 관리자 권한 진입 제약**:
   - Viewer 프로그램은 관리자 로그인을 통해서만 메인 UI에 진입할 수 있도록 제어합니다.
2. **원격 접속 연동형 장비 자동 등록**:
   - Agent 로그인 계정 정보(아이디 = 사업자번호 `000-00-00000` 포맷, 패스워드 = `1234`)를 Viewer에서 입력하여 원격 접속을 시작함과 동시에, 해당 Agent 장비가 관리자 계정의 '관리할 장비 리스트'에 자동으로 추가/등록되는 파이프라인을 지원합니다.
3. **업장/장비 그룹핑 및 상세 메타데이터 입력**:
   - 한 업장에서 여러 대의 장비를 연동하여 사용할 수 있으므로 장비 리스트를 그룹별로 분류/관리할 수 있는 그룹핑 기능을 제공합니다.
   - 장비 리스트에는 각 장비별로 '장비 번호' 및 '장비명'을 기재할 수 있는 커스텀 필드(수동 입력 칸)를 제공합니다.
4. **데스크탑 이름 자동 수집**:
   - 원격 연동 시 Agent 실행 환경의 Windows 컴퓨터 이름(데스크탑 호스트명)을 API를 통해 자동으로 수집하여 리스트 상에 자동으로 기입해 줍니다.

MVP에는 다음 기능을 넣지 않는다.

- UAC/잠금화면 완전 제어
- 무인 접속
- 파일 전송
- 녹화
- 오디오 전달
- 관리자 웹
- 조직/권한 정책
- 모바일 앱

## 6. 중단 조건

다음 중 하나라도 발생하면 개발 방향을 재검토한다.

- PoC가 기존 제품보다 체감상 빠르지 않다.
- J1800/J1900 + RAM 2GB 장비에서 Agent CPU가 지속적으로 50%를 넘거나 메모리 피크가 300MB를 넘는다.
- `1024x768`에서도 입력 지연이 기존 제품보다 낮지 않다.
- `1600x900` 또는 `1080x1920`에서 화질을 낮춰도 조작이 끊긴다.
- Relay 의존도가 높아 운영 비용이 감당되지 않는다.
- 주요 고객 환경에서 UDP 연결 성공률이 낮다.
- Windows 권한 제어 요구가 MVP 범위를 계속 침범한다.
- 보안/백신 오탐을 해결할 코드 서명과 배포 체계를 확보하지 못한다.
- 한 명 또는 두 명이 전체 제품을 짧은 기간에 만들겠다는 일정으로 바뀐다.
- 법적 검토 없이 기존 제품의 UI, 자산, 프로토콜, 동작을 그대로 맞추려 한다.

## 7. 권장 아키텍처

### 7.1 전체 구성

1. Agent
   - 원격 제어를 받는 PC에 설치
   - 화면 캡처
   - 가능하면 하드웨어 인코딩 사용
   - 하드웨어 인코딩 실패 시 저해상도/저FPS/타일 전송 fallback
   - 입력 주입
   - 세션 승인/종료
   - 로컬 로그 기록

2. Viewer
   - 원격 접속을 수행하는 PC에서 실행
   - 관리자 로그인 인증을 거쳐야만 진입 가능
   - 관리할 장비 리스트(그룹핑, 장비 번호, 장비명, 자동 획득한 데스크탑 이름 표시) 관리 UI 제공
   - Agent 계정 정보(사업자번호/비밀번호) 입력 시 즉시 원격 접속 개시 및 장비 리스트 자동 등록 요청 송신
   - 화면 디코딩 및 렌더링
   - 입력 이벤트 전송
   - 품질 설정
   - 연결 상태 표시

3. Signaling 서버
   - 접속 코드 발급
   - Agent/Viewer 매칭
   - 세션 인증
   - WebRTC 연결 협상

4. Relay/TURN 서버
   - 직접 연결 실패 시 트래픽 중계
   - 운영 비용이 크므로 사용률을 반드시 측정

5. API/Admin 서버
   - MVP 이후 추가
   - 관리자 계정 정보 관리 및 인증 처리
   - Agent 계정(사업자번호 및 패스워드) 정보 관리
   - Viewer의 접속 및 장비 등록 연계 처리 (원격 접속 시 장비 정보 자동 등록 API 제공)
   - 장치 그룹핑, 장비 번호, 장비명, 데스크탑 이름 등 장치 메타데이터 DB 관리
   - 계정, 장치, 정책, 로그, 업데이트 관리

### 7.2 기술 선택

Desktop Core:

- Rust 우선 검토
- Windows API 연동이 까다로운 부분은 C++ 모듈 허용
- 화면 캡처, 인코딩, 입력 주입은 UI와 분리
- Agent는 반드시 경량 네이티브 프로세스로 만든다.
- Agent에는 Electron, Chromium 내장 UI, 무거운 웹뷰 런타임을 넣지 않는다.
- Agent UI가 필요하면 Win32/WinUI 최소 UI 또는 별도 경량 설정창으로 제한한다.

UI:

- 초기 PoC는 UI 최소화
- MVP부터 Viewer에는 Qt, Tauri, 또는 네이티브 UI 검토
- Agent는 저사양 PC 메모리 제약 때문에 네이티브 경량 UI를 우선한다.
- 화면 렌더링 성능이 중요하므로 UI 프레임워크보다 렌더링 파이프라인을 우선 검증

Media/Transport:

- **동등 후보군 (A/B Test 비교군):**
  - **후보 1: WebRTC** (J1800/J1900 + RAM 2GB에서 다중 스레드 및 메모리 오버헤드가 합격 기준 안에 들어올 때만 채택)
  - **후보 2: QUIC 또는 UDP 기반 자체 경량 단일 스레드 전송** (저사양 Agent의 CPU 부하 방어를 위한 주력 대안)
- 3순위: TCP/TLS 저품질 fallback
- PoC 3주차에 동일 화면, 동일 J1800 장비, 동일 네트워크 조건에서 CPU 사용률, 메모리 사용량, 스레드 context switching, p95/p99 E2E 지연을 비교해 최종 전송 방식을 결정한다.

Server:

- Go 또는 Rust
- PostgreSQL
- Redis
- TURN 서버는 coturn 같은 검증된 구현 우선 검토

## 8. 저지연 파이프라인 설계

목표는 각 단계의 지연을 합쳐 관리하는 것이다.

1. 화면 캡처
   - Windows Graphics Capture API 우선 검토
   - Desktop Duplication API fallback
   - 캡처 지연과 누락 프레임 측정
   - 전체 화면을 매 프레임 처리하지 말고 변경 영역 감지와 타일 단위 처리를 우선 검토

2. 인코딩
   - H.264 low-latency 프로파일 우선
   - NVENC, Intel Quick Sync, AMD AMF 지원 (지원 장비에 한함)
   - J1800/J1900에서는 Quick Sync H.264 인코딩 사용 가능을 전제로 잡지 않고 드라이버별로 직접 검증
   - GPU 인코딩이 불안정하면 CPU 인코딩 fallback을 제공하되 해상도와 FPS를 강제로 낮춤
   - B-frame 비활성화, 짧은 GOP, 낮은 버퍼링 적용
   - CPU 인코딩 fallback은 `1024x768` 또는 축소 해상도 중심으로 설계
   - 정적인 업무 화면은 H.264 고정 프레임보다 변경 타일 기반 압축이 더 나을 수 있으므로 PoC에서 비교

3. 전송
   - UDP 기반 저지연 전송 우선(WebRTC 또는 자체 경량 전송 후보)
   - 입력 이벤트는 영상 프레임보다 높은 우선순위
   - 커서는 영상에 굽지 말고 별도 데이터로 전송
   - 정지 화면에서는 프레임률과 비트레이트 감소
   - 스크롤/드래그 중에는 품질보다 최신 프레임과 입력 반응을 우선

4. 디코딩/렌더링
   - Viewer에서 하드웨어 디코딩 우선
   - 렌더 큐를 짧게 유지
   - 지연이 늘어나면 화질보다 최신 프레임을 우선 표시

### 8.1 저사양 Agent 전용 성능 프로파일

원격 제어 대상 PC가 `J1800/J1900 + RAM 2GB`라면 기본 프로파일을 다음처럼 잡는다.

| 해상도 | 기본 목표 | 모션 중 목표 | 비관적 fallback |
| --- | --- | --- | --- |
| `1024x768` | 15~20fps | 10~15fps | 8fps, 품질 낮춤 |
| `1600x900` | 10~15fps | 8~12fps | 1280x720 축소 |
| `1080x1920` | 8~12fps | 5~10fps | 720x1280 축소 또는 타일 우선 전송 |

Agent 리소스 목표 예산 (J1800/J1900 실장비 직접 측정 전까지 미검증):

- 평상시 메모리: 150MB 이하 (목표값, 실장비 직접 측정 전까지 미검증)
- 순간 피크 메모리: 250MB 이하 (목표값, 실장비 직접 측정 전까지 미검증)
- 일반 조작 중 CPU: 40% 이하 (목표값, 실장비 직접 측정 전까지 미검증)
- 스크롤/드래그 중 CPU 피크: 70% 이하, 5초 이상 지속 금지 (목표값, 실장비 직접 측정 전까지 미검증)
- 디스크 스왑 발생 시 실패로 간주

화질 정책:

- 텍스트 가독성을 프레임률보다 우선한다.
- 움직이는 동안에는 해상도/품질을 낮추고, 멈추면 선명도를 회복한다.
- 커서는 항상 별도 전송해 저FPS에서도 조작감을 유지한다.
- 전체 화면 영상 스트리밍보다 변경 영역/타일 전송을 우선 검증한다.

### 8.2 J1800/J1900 OS/드라이버 인벤토리 및 캡처 Fallback 상세 기준

DXGI, WGC, Quick Sync, CPU fallback 중 어떤 경로를 사용할지는 실제 장비 인벤토리로 확정하며, OS 빌드 및 캡처 메커니즘을 다음과 같이 세밀하게 정의한다.

- **OS 상세 기준:**
  - Windows 10 64-bit (Redstone 4 / 빌드 1803 이상)을 1단계 baseline으로 둔다.
  - Windows 7은 DXGI/WGC/드라이버 제약이 커서 1단계 지원 대상에서 완전히 배제한다.
- **WGC(Windows Graphics Capture) 가동 조건:** Windows 10 빌드 1803 이상이 요구되며, 화면 노란색 테두리(Capture Border) 강제 표시 제거를 위해 `IsBorderRequired = false`를 적용하려면 **Windows 10 빌드 2004 (20H1) 이상**의 OS 환경이어야 함.
- **DXGI Desktop Duplication 가동 조건:** Windows 8 이상, WDDM 1.3 이상을 최소 확인 기준으로 둔다.
- **드라이버 실패 시 Fallback 경로 (캡처 예외 처리):**
  - 만약 대상 장비의 디스플레이 드라이버 결함(예: 가상 드라이버 Mirage Driver 활성화 등으로 인한 DXGI/WGC 초기화 실패) 시, CPU 기반 **GDI 캡처(BitBlt / GetDC)** 방식으로 자동 Fallback하도록 강제한다.
  - GDI Fallback 진입 시 CPU 자원 소모 방지를 위해 강제로 캡처 프레임 레이트를 **5~8 FPS**로 고정하고, 16비트 RGB565 변환 및 변경 영역 타일링을 결합 적용한다.
- **GPU 드라이버:** Intel HD Graphics Driver 15.33 계열 최종 릴리즈 드라이버를 우선 확인한다.
- **인벤토리 항목:** OS 빌드, 32/64비트, DirectX 버전, WDDM 버전, GPU 드라이버 버전, BIOS 설정, 하드웨어 인코더 노출 여부.
- **Intel QuickSync 검증:** Intel 공식 사양에 Quick Sync Video 항목이 있더라도, 실제 H.264 인코더 사용 가능 여부는 시스템/BIOS/드라이버/런타임에 따라 달라질 수 있으므로 PoC에서 직접 판정한다.
- 기본 검증 모델은 Quick Sync 의존이 아니라 CPU 타일 차분 압축으로 잡는다.
- Quick Sync H.264 인코딩이 안정적으로 동작하는 장비에서는 선택적 가속 경로로만 사용한다.

### 8.3 로컬 캡처/RGB565 변환 벤치마크 해석

1920x1200 해상도에서 진행한 로컬 벤치마크 결과는 다음과 같다.

테스트 환경:

- CPU: 11th Gen Intel Core i5-11400 @ 2.60GHz
- RAM: 16.0GB
- GPU: Intel UHD Graphics 730
- 추가 표시 드라이버: Mirage Driver가 함께 감지됨
- OS: Windows 11 Pro 64-bit, build 26100
- 빌드 구성: Release profile (`cargo build --release`)
- 실행 파일: `C:\Users\qpalz\.gemini\antigravity\scratch\aether-link-poc\target\release\wonremote-poc.exe`
- 측정 시간: 5.0초

주의: Mirage Driver 같은 가상 디스플레이 드라이버가 감지되는 환경에서는 DXGI가 실제 어떤 adapter/output을 선택했는지 별도 로그로 남겨야 한다.

현재 기준 작업본과 검증 산출물:

- workspace PoC: `C:\Users\qpalz\Documents\remote\aether-link-poc`
- scratch 원본은 참고용: `C:\Users\qpalz\.gemini\antigravity\scratch\aether-link-poc`
- machine-readable 벤치마크 결과: `C:\Users\qpalz\Documents\remote\aether-link-poc\benchmark_results.json`
- Week 2 감사 문서: `C:\Users\qpalz\Documents\remote\aether-link-poc\WEEK2_AUDIT_REPORT.md`
- schema v2 기준 실행 옵션: `--duration`, `--output`, `--snapshot`, `--loop-sleep-ms`, `--capture-timeout-ms`
- schema v2 추가 기록: 프로세스 CPU/메모리/thread, user/kernel CPU split, 캡처 루프 timeout/access lost/error/dirty frame 통계

- 테스트 해상도: `1920x1200`
- 평균 프레임레이트: 60.56fps (1단계 PoC 화면 캡처 및 RGB565 변환 단독 결과 - 타일 차분, 압축 및 전송 제외)
- DXGI 화면 캡처 지연: 평균 5.182ms, 최소 2.86ms, 최대 16.39ms (1단계 PoC 단독)
- RGB565 변환 지연: 평균 1.940ms, 최소 1.77ms, 최대 2.88ms (1단계 PoC 단독)
- 캡처+변환 합산 내부 처리 지연: 평균 7.123ms (1단계 PoC 단독)
- 32비트 BGRA 원본 프레임 크기: 8.79MB
- 16비트 RGB565 변환 프레임 크기: 4.39MB
- 원시 프레임 기준 데이터량 절감: 50.0%
- 메모리 버스 처리 대역폭: 532.30MB/s

2026-06-09 workspace 재실행 결과는 `benchmark_results.json`을 기준으로 별도 보존한다. 이 실행(2단계 PoC 타일 차분 및 JPEG 압축 적용)에서는 `1920x1200`, `Intel UHD Graphics 730`, `Mirage Driver 감지` 조건에서 평균 `15.77fps`, internal pipeline 평균 `17.35ms`, p95 `40.97ms`, p99/max `111.88ms`가 기록되었다. (주의: 이 결과는 타일 차분과 JPEG 압축 루프가 포함되어 연산량이 증가하였고, Mirage Driver 감지 등으로 캡처가 지연되어 FPS가 15.77fps로 저하된 것이며, "안정적 60fps"가 아님을 확인하였다.)

픽셀 수 기준 단순 환산 (Week 1 단독 변환 기준 단순 비례 계산 - 참고용):

| 대상 해상도 | 1920x1200 대비 픽셀 비율 | RGB565 변환 시간 단순 추정 (검증 필요) |
| --- | ---: | ---: |
| `1024x768` | 34.1% | 약 0.66ms (검증 필요) |
| `1600x900` | 62.5% | 약 1.21ms (검증 필요) |
| `1080x1920` | 90.0% | 약 1.75ms (검증 필요) |

보수적 해석 및 미검증 항목:

- 이 결과는 로컬 고사양 PC에서의 부분 검증 데이터일 뿐이며, 아래 핵심 요소들은 직접 측정 전까지 **검증 필요**로 분류한다.
  1. **J1800/J1900 + RAM 2GB 실장비 성능:** Celeron J1800/J1900 실장비에서 직접 측정한 결과가 없으므로 해당 스펙에서의 동작 및 리소스(CPU, RAM) 점유율은 전부 **검증 필요** 상태임. (저사양 Agent 10ms 이하 지연은 입증되지 않음)
  2. **기존 ZOOK 대비 개선 여부:** 기존 ZOOK의 지연 시간 및 성능 baseline 측정이 수행되지 않아 기존 대비 개선 여부는 **검증 필요** 상태임.
  3. **Visual Ping E2E 지연:** 입력 주입, 네트워크 전송, 디코딩, Viewer의 `T_presented` 렌더링을 포함한 최종 E2E 지연은 **검증 필요** 상태임.
- 실제 PoC 판정은 J1800/J1900 + RAM 2GB 장비에서 동일 계측을 반복해 p95/p99와 최대값까지 확인해야 한다.
- RGB565의 50% 절감은 원시 프레임 크기 기준이다. 최종 네트워크 전송량은 타일 차분, 압축, 패킷 헤더, 재전송, Relay 여부에 따라 달라진다.
- `1920x1200` 전체 프레임을 RGB565로 60fps 전송하면 약 263MB/s 수준의 원시 대역폭이 필요하므로 원격접속 전송 방식으로 사용할 수 없다.
- 전체 프레임 RGB565 단순 direct streaming은 금지한다.
- RGB565는 캡처 후 변경된 dirty tile 버퍼링과 타일 단위 압축 전처리용 픽셀 포맷으로만 사용한다.
- 실제 전송에는 Tile-diff/Dirty Rects, 차분 압축, 무변화 프레임 스킵, motion 중 품질 저하가 필수다.
- 텍스트 가독성은 육안 비교만으로 확정하지 않고, OCR 성공률, 확대 비교, SSIM/PSNR, 작은 글자 UI 샘플로 별도 검증한다.
- 이 벤치마크는 캡처, 색 변환, 메모리 복사 경로만 다룬다. 입력 주입, 타일 diff, 네트워크 전송, 디코딩, 렌더링, TURN Relay 지연은 Visual Ping으로 따로 검증한다.

### 8.4 네트워크 결합 전 필수 측정 보강 계획

1. **J1800/J1900 병목 분석용 추가 지표 수집:**
   - **컨텍스트 스위칭(Context Switching) 및 스레드 수 계측:** WebRTC의 다중 스레드에 의한 J1800 CPU 오버헤드를 대비하기 위해 Agent 프로세스의 active 스레드 수 및 초당 컨텍스트 스위칭 발생 빈도 측정 추가.
   - **물리 메모리 점유율(Working Set) 프로파일링:** 2GB RAM 제약 조건 하에서 메모리 누수 또는 과도한 페이지 폴트 발생 여부를 감지하기 위해 작업 세트(Private Working Set) 크기 측정 추가.
   - **CPU 커널/유저 타임 계측:** Agent의 CPU 소비가 순수 유저 연산(JPEG 인코딩) 때문인지, 아니면 커널 영역의 Syscall(DXGI, GDI 캡처)이나 드라이버 경합 때문인지 확인하기 위해 Kernel/User CPU Time 분리 계측 추가.

2. **FPS 수치 충돌(60.56 vs 47.79 vs 15.77) 표준 격리 측정 방안:**
   - **화면 자극(Screen Stimulus) 표준화 프로토콜:** DXGI Duplication API의 타임아웃/무변화 자동 드롭 특성으로 인한 FPS 요동을 방지하기 위해, 벤치마크 수행 시 다음 2가지 모드로 화면 변경 상황을 제어하여 측정한다.
     - **Static Mode (정적 오버헤드 계측):** 움직임이 전혀 없는 바탕화면 상태에서 FPS(0 FPS 목표) 및 CPU/RAM 점유율 실측.
     - **Controlled Dynamic Mode (표준 60Hz 자극):** 60fps로 제작된 표준 테스트 비디오(예: Blender 오픈 퓨티지 영상)를 지정 해상도 창에서 전체 화면으로 재생하는 동안 FPS 및 파이프라인 지연 계측.
   - **실행 모드(Execution Flags)의 구조적 분리:** 시뮬레이션 부하(Case B/C)나 디코딩 렌더 루프 유무에 따라 기본 FPS가 간섭받는 것을 방지하기 위해, CLI 인수를 통해 단독 파이프라인 구간별 측정을 명시적으로 격리 지원한다. (예: `--mode capture-only`, `--mode encode-only`, `--mode loopback-full`)

## 9. Windows 권한과 보안 데스크톱

가장 위험한 영역이다. 처음부터 완전 지원을 약속하면 일정이 무너진다.

MVP 지원:

- 일반 사용자 세션의 화면 보기
- 일반 앱의 마우스/키보드 제어
- 사용자 승인 기반 접속

MVP 제외:

- UAC 보안 데스크톱 제어
- 잠금화면 제어
- 로그인 화면 제어
- 무인 접속
- 드라이버 기반 입력/캡처

후속 검토:

- Windows 서비스와 사용자 세션 프로세스 분리
- UIAccess 서명
- 코드 서명 인증서
- 권한 상승 Helper
- 명시적 사용자 동의
- 감사 로그

## 10. 보안 원칙

원격제어 프로그램은 악용 가능성이 높다. 보안은 부가기능이 아니라 제품의 생존 조건이다.

- 모든 세션은 종단 간 암호화 또는 그에 준하는 암호화 적용
- 접속 코드는 짧은 유효기간과 1회성 사용 원칙 적용
- Agent 화면에 접속 상태를 항상 표시
- 사용자가 즉시 세션을 종료할 수 있어야 함
- 무인 접속은 MVP에서 제외
- 파일 전송과 클립보드는 기본 비활성화
- 세션 시작/종료/접속자/대상 장치/승인 여부를 로그로 남김
- 업데이트 파일은 서명 검증 후 실행
- 관리자 권한 기능은 별도 보안 설계 완료 전까지 제한

## 11. 법적/라이선스 원칙

허용:

- 기존 제품을 실행해 사용자 관점의 기능을 관찰
- 화면 흐름과 기능 목록 작성
- 성능 비교
- 공개 문서와 공개 API 기반 재구현

금지:

- 기존 실행 파일 디컴파일
- 바이너리 패치
- UI 이미지, 아이콘, 문구 복사
- 비공개 프로토콜 복제
- 라이선스 조건을 검토하지 않은 오픈소스 포함

권장:

- Clean-room 방식으로 명세와 구현자를 분리
- 기능 명세에는 관찰 가능한 동작만 기록
- 구현은 새 프로토콜, 새 UI, 새 자산으로 진행

## 12. 개발 단계

### 0단계: 기준 측정

기간: 1~2주

목표:

- 기존 제품이 실제로 어느 정도 느린지 수치로 확인
- 비교 대상 제품의 기준값 확보

작업:

- ZOOKAgent/ZOOKViewer 기능 목록 작성
- AnyDesk, RustDesk, Chrome Remote Desktop과 비교
- LAN, 일반 인터넷, 테더링 환경 테스트
- 지연, FPS, CPU/GPU, 메모리, 끊김, 재연결 시간 측정
- `J1800`, `J1900`, RAM 2GB 장비를 실제 테스트 장비로 확보
- `1024x768`, `1600x900`, `1080x1920` 세 해상도에서 각각 기준 측정

산출물:

- 기능 매트릭스
- 성능 기준표
- 테스트 영상
- PoC 합격 기준 확정
- 저사양 Agent 성능 예산표

### 1단계: 저지연 엔진 PoC

기간: 4~6주

목표:

- UI 없이 핵심 엔진만 검증

포함:

- 화면 캡처
- 하드웨어 인코딩
- WebRTC 및 자체 경량 전송 A/B 비교 테스트
- Viewer 렌더링
- 입력 이벤트 왕복
- 지연 측정 도구
- 저사양 프로파일 자동 전환

제외:

- 예쁜 UI
- 설치 프로그램
- 채팅
- 파일 전송
- 녹화
- 계정 관리

### 2단계: MVP

기간: 8~12주

목표:

- 제한된 환경에서 실제 사용 가능한 제품
- 저사양 Agent PC에서 장시간 실행해도 메모리 증가와 스왑이 없을 것

포함:

- Agent 설치/실행
- Viewer 실행
- 접속 코드 기반 연결
- 원격 화면 보기
- 마우스/키보드 제어
- 다중 모니터 선택
- 품질 자동 조절
- 저사양 모드 수동/자동 전환
- 세션 종료
- 기본 로그

### 3단계: 기존 제품 대체 기능

기간: 12~20주

추가:

- 채팅
- 클립보드 공유
- 파일 전송
- 세션 녹화
- 오디오 전달
- 자동 업데이트
- 접속 승인 UI 개선
- 연결 이력

### 4단계: 운영/상용화

기간: 12주 이상

추가:

- 관리자 로그인 및 Agent 연동 계정 관리
- 원격 접속 연동형 장비 리스트 자동 등록 기능
- 장치 그룹핑, 커스텀 장비 번호/장비명 필드 지원
- Agent 데스크탑 호스트명 자동 수집 및 DB 연동
- 계정/조직 관리 및 권한 정책
- 무인 접속
- 관리자 웹 및 중앙 로그
- 코드 서명
- 취약점 점검
- 설치/업데이트 안정화
- Relay 비용 최적화

## 13. 테스트 계획

### 13.1 최악 조건 조합 테스트 매트릭스 (Worst-Case Combination Matrix)
독립 변수를 따로 테스트하지 않고, 아래의 최악 조건을 조합한 매트릭스를 구성한다. `Worst-01`은 PoC 필수 통과 게이트이며, 이를 통과하지 못하면 다른 조건이 좋아도 PoC 성공으로 판정하지 않는다.

| 테스트 케이스 | 장비 사양 | 화면 해상도 및 모드 | 네트워크 연결 환경 | 인코딩 모드 및 조건 | 통과 기준 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **Worst-01** | Celeron J1800 <br> RAM 2GB | 1080x1920 (세로형) | Wi-Fi 연결 + TURN Relay 중계 <br>(Packet Loss 3%, RTT 60ms) | CPU Software Fallback <br>(가변 해상도/화질 강하) | p95 E2E 지연 180ms 이하 <br>p99 E2E 지연 300ms 이하 <br>최대값 500ms 이하 <br>CPU 50% 이하 유지 <br>메모리 피크 300MB 이하 |
| **Worst-02** | Celeron J1900 <br> RAM 2GB | 1600x900 (가로형) | Wi-Fi 연결 + TURN Relay 중계 <br>(Packet Loss 5%, RTT 40ms) | CPU Software Fallback <br>(가변 해상도/화질 강하) | p95 E2E 지연 150ms 이하 <br>p99 E2E 지연 200ms 이하 <br>최대값 350ms 이하 <br>CPU 40% 이하 유지 <br>메모리 피크 250MB 이하 |

### 13.2 환경 조건 및 성능 테스트 범위
- LAN / Wi-Fi / 일반 인터넷 / 4G/5G 테더링
- RTT 100ms 이상
- 패킷 손실 1%, 3%, 5%
- UDP 차단 환경 (Symmetric NAT)
- 로컬 캡처/RGB565 변환 기준 테스트: `1920x1200`

기기 테스트:

- Celeron J1800, RAM 2GB
- Celeron J1900, RAM 2GB
- Intel 내장 GPU
- NVIDIA GPU
- AMD GPU
- GPU 인코딩 불가 PC
- 단일 모니터
- 다중 모니터
- 고DPI 디스플레이
- `1024x768`
- `1600x900`
- `1080x1920`
- `1920x1200`

Windows 테스트:

- Windows 10
- Windows 11
- 관리자 권한 앱
- 일반 권한 앱
- 절전 복귀
- 화면 잠금 전후

품질 지표:

- p50/p95/p99 입력 지연
- p50/p95/p99 프레임 지연
- FPS
- 비트레이트
- 프레임 드롭
- 재연결 시간
- CPU/GPU/메모리 사용률
- Agent working set
- 스왑 발생 여부
- 장시간 실행 후 메모리 증가량
- 해상도별 자동 fallback 횟수
- Relay 사용률
- RGB565 적용 후 OCR 성공률
- RGB565 적용 전후 SSIM/PSNR

## 14. 리스크 매트릭스

| 리스크 | 심각도 | 가능성 | 대응 |
| --- | --- | --- | --- |
| 기존 제품보다 빠르지 않음 | 매우 높음 | 중간 | PoC에서 즉시 판정 |
| J1800/J1900에서 CPU 인코딩 불가 | 매우 높음 | 높음 | 해상도 축소, 타일 전송, Quick Sync 검증 |
| RAM 2GB에서 Agent 메모리 초과 | 매우 높음 | 높음 | Agent 경량 네이티브화, 메모리 예산 강제 |
| `1080x1920` 세로 화면 처리 지연 | 높음 | 높음 | 저FPS/축소/타일 우선 fallback |
| WebRTC 런타임 오버헤드 과다 | 높음 | 중간 | 자체 경량 전송 후보와 A/B 비교 |
| UAC/잠금화면 제어 난이도 | 높음 | 높음 | MVP 제외 |
| TURN Relay 비용 증가 | 높음 | 중간 | 연결 성공률과 Relay 비율 측정 |
| 백신/EDR 오탐 | 높음 | 중간 | 코드 서명, 투명한 설치/동의 UX |
| GPU 인코딩 호환성 | 중간 | 높음 | CPU fallback, 테스트 매트릭스 확대 |
| WebRTC 네이티브 통합 난이도 | 높음 | 중간 | PoC에서 라이브러리 확정 |
| 보안 사고 가능성 | 매우 높음 | 낮음~중간 | 무인 접속 제한, 로그, 인증 |
| 일정 과소평가 | 높음 | 높음 | 단계별 중단 조건 적용 |

## 15. 인력과 일정

현실적인 최소 인력:

- Windows 네이티브 개발자 1명
- 미디어/WebRTC 개발자 1명
- 백엔드 개발자 1명
- 데스크톱 UI 개발자 1명
- QA/성능 테스트 담당 1명

소규모 인력으로 진행할 경우:

- 1~2명: PoC까지만 현실적
- 3명: 제한된 MVP 가능
- 5명 이상: 기존 제품 대체 수준 가능

현실적 일정:

- 기준 측정: 1~2주
- PoC: 4~6주
- 제한 MVP: 3~4개월
- 기존 제품 대체 수준: 8~12개월
- 상용 운영 수준: 12개월 이상

6개월 안에 상용 수준을 목표로 하면 범위 축소가 필수다.

## 16. 최우선 작업 목록

9차 기준 운영 우선순위 조정: 실장비 성능 검증은 즉시 차단 게이트가 아니라 후순위 검증 게이트로 둔다. 먼저 관리자 로그인, Agent 계정 기반 접속, 장비 자동 등록, 장비 리스트 관리, 기본 원격 화면 보기/입력 제어가 이어지는 사용 가능한 프로그램 흐름을 만든 뒤, 프로그램이 무겁거나 지연이 과도할 때 J1800/J1900 실장비 병목을 집중 검증한다. 단, 실장비 측정 전까지 저사양 장비 성능 합격 판정은 내리지 않는다.

1. Viewer 관리자 로그인 화면과 메인 장비 관리 화면의 최소 동작 버전을 만든다.
2. Agent 계정 정보(사업자번호/비밀번호), 장비 번호, 장비명, Windows 컴퓨터 이름 수집 흐름을 구현한다.
3. Viewer에서 Agent 접속 시 장비 리스트에 자동 등록되는 로컬/개발용 연결 흐름을 만든다.
4. 기본 원격 화면 보기와 마우스/키보드 입력 제어의 end-to-end 세션 골격을 만든다.
5. Visual Ping 기반 성능 측정 도구를 프로그램 흐름 안에 붙인다.
6. 기존 ZOOK의 기능과 지연을 측정한다.
7. 프로그램이 무겁거나 지연이 과도하면 J1800/J1900 실장비에서 병목을 재측정한다.
   - 기준 PoC: `C:\Users\qpalz\Documents\remote\aether-link-poc`
   - 결과 파일: `benchmark_results.json`을 환경별로 보존한다.
8. Windows 화면 캡처 방식을 확정한다.
9. Quick Sync, CPU 인코딩, 타일 기반 전송을 같은 장비에서 비교한다.
10. WebRTC와 자체 경량 전송 후보를 A/B 비교한다.
11. 입력 이벤트 왕복 지연을 측정한다.
12. 기존 제품, AnyDesk, RustDesk, Chrome Remote Desktop과 비교한다.
13. PoC 합격 여부를 수치로 판단한다.

## 17. 최종 제안

이 프로젝트의 성공 가능성을 높이는 유일한 방법은 범위를 줄이는 것이다.

처음 목표는 "ZOOK의 모든 기능을 더 빠르게 복제"가 아니라 "`J1800/J1900 + RAM 2GB`에서 원격 화면 보기와 마우스/키보드 제어가 기존보다 빠른 핵심 엔진 증명"이어야 한다.

핵심 엔진이 성공하면 제품화는 가능하다. 핵심 엔진이 실패하면 채팅, 녹화, 파일 전송, 관리자 웹을 아무리 잘 만들어도 좋은 원격접속 제품이 되지 않는다.

따라서 다음 의사결정이 필요하다.

- 6주 PoC 예산과 시간을 먼저 배정한다.
- 저사양 테스트 장비 없이는 성능 합격 판정을 내리지 않는다.
- PoC 합격 기준을 수치로 고정한다.
- MVP 범위에서 고위험 기능을 제거한다.
- 기존 제품 복제 표현을 버리고 독립 구현 제품으로 정의한다.
- 성능, 보안, 법적 리스크 중 하나라도 감당 불가능하면 개발을 중단한다.

## 18. 작업 이력 및 수차 기록

- **1차 개정 (2026-06-09):** CodeX 지연 측정 개선안 수용 (T_decode_detect / T_presented 분리 및 최종 공식 E2E Latency = T_presented - T_start 적용, i5-11400 벤치마크 환경 세분화, RGB565 전체 스트리밍 금지 및 대역폭 경고 추가, Worst-01/02 절대 지연 상한 설정).
- **2차 개정 (2026-06-09):** 작업 품질 가드레일(`GEMINI_QUALITY_GUARDRAILS.md`) 정립 및 빌드/형상 검증 완료.
- **3차 개정 (2026-06-09):** `GEMINI_HANDOFF.md` 지침에 따른 수치 충돌 분석 및 과장 표현 조정. 1단계 단독 결과(60.56 FPS)와 2단계 full-pipeline 결과(15.77 FPS) 분리 정의 및 미검증 항목(J1800 실장비 성능, 기존 ZOOK 비교, Visual Ping E2E)에 대한 `(검증 필요)` 명시 보강.
- **4차 개정 (2026-06-09):** 3주차 네트워크 통합 전 필수 측정 보강 계획(Section 8.4) 추가 (컨텍스트 스위칭, Working Set, CPU Kernel/User Time 분리 계측, 정적/동적 화면 자극 표준화 프로토콜, 실행 모드 격리 측정 설계 반영).
- **5차 개정 (2026-06-09):** 최종 관리 및 연결 기능에 대한 사용자 신규 요구사항 반영 (관리자 로그인 제약, 에이전트 계정 기반 접속 및 장비 자동 등록, 업장별 그룹핑, 장비 번호/장비명 필드 제공, 컴퓨터명 자동 획득 등 최종 목표 반영).
- **6차 개정 (2026-06-09):** 사용자 지시에 따라 실장비 성능 검증을 즉시 차단 게이트에서 후순위 검증 게이트로 조정. 다음 단계는 성능 실측 패키지보다 관리자 로그인, Agent 계정 기반 접속, 장비 자동 등록, 기본 원격 세션 골격을 먼저 구현하는 방향으로 변경.
- **7차 개정 (2026-06-09):** `aether-link-app` Viewer 골격 구현 착수. 관리자 로그인, Agent 접속 입력, 자동 장비 등록, 업장별 장비 리스트, 기본 원격 세션 패널을 로컬 앱 상태로 구현.
- **8차 개정 (2026-06-09):** 최종 원격 장비 관리 및 접속 기능에 대한 신규 요구사항(관리자 로그인 한정 진입, 사업자번호/비밀번호 에이전트 계정 정보 입력을 통한 원격 접속 및 리스트 자동 등록, 업장별 그룹핑, 장비 번호/장비명 필드, 데스크탑 이름 자동 획득)을 개발 계획서(`ZOOK_REPLACEMENT_PLAN.md`)에 설계 반영.
- **9차 개정 (2026-06-10):** CodeX 기준 9차 운영 우선순위 조정 결과 반영. 실장비 성능 검증 대신 Viewer UI 골격 및 연결 제어 흐름 구현의 우선순위 공식 적용.
- **10차 개정 (2026-06-10):** Viewer 골격 및 접속/그룹핑 흐름 구현 상태 검증. 9차 우선순위 정렬 부합 여부 확인.
- **11차 개정 (2026-06-10):** Agent 비밀번호 개발 기준 `1234` 엄격 검증 로직 추가. wrong password 시 장비 등록 및 세션 패널 연결 차단, `1234` 입력 시에만 자동 등록 및 연결 처리 반영. 현재 앱이 실제 스트리밍/OS 입력 주입이 배제된 UI 상태 머신 및 이벤트 로그 프로토타입 상태임을 명시.
- **12차 개정 (2026-06-10):** `aether-link-app`에 Node 로컬 API 서버를 추가하고 Viewer가 관리자 로그인, 장비 목록 조회, Agent 연결, 세션 생성, 입력 이벤트 기록을 HTTP API로 호출하도록 조정. wrong password는 Viewer에서 API 요청 전 차단하고, 서버 API도 직접 요청에 대해 `1234` 검증을 유지한다. 상태 저장은 아직 메모리 기반이며 실제 화면 스트리밍/OS 입력 주입은 다음 구현 범위로 유지.
- **13차 개정 (2026-06-11):** Agent 최초 실행 등록 흐름 추가. `/?mode=agent` Agent 화면에서 아이디(사업자번호)와 비밀번호만 입력하면 설치 식별자를 기반으로 장비 번호/장비명을 파생해 `/api/agent/first-run`으로 등록하고, Viewer는 로그인 상태에서 장비 목록을 주기 갱신하여 신규 Agent를 자동 표시한다. wrong password의 Viewer 측 사전 API 요청 차단 및 도메인 단위 테스트 보강을 마침. 현재 Agent 화면은 웹 프로토타입이며 설치 식별자는 브라우저 localStorage에 보관한다. 설치형 Agent EXE/Windows 서비스, 실제 화면 스트리밍, OS 입력 주입은 아직 구현되지 않음.
- **14차 개정 (2026-06-11):** 로컬 Agent 부트스트랩 CLI(`npm run agent`) 추가. 최초 실행 시 아이디(사업자번호)와 비밀번호를 입력받아 `/api/agent/first-run`으로 Viewer 장비 목록에 등록하고, 설치 식별자/등록 장비 ID는 로컬 config 파일에 저장한다. `WONREMOTE_AGENT_ID`, `WONREMOTE_AGENT_PASSWORD`, `WONREMOTE_AGENT_CONFIG` 환경변수 기반 무인 등록 경로를 추가했으며, 등록 완료 config가 있으면 재실행 시 비밀번호를 다시 묻지 않는다. 아직 설치형 EXE/Windows 서비스 등록, 영구 서버 DB, 실제 스트리밍/OS 입력 주입은 구현되지 않음.
- **15차 개정 (2026-06-11):** 로컬 API 서버에 파일 기반 장비 저장소 추가. `/api/agent/first-run` 및 `/api/agent/connect`로 등록/갱신된 장비 목록을 JSON 파일에 저장하고, API 서버 재시작 후 `/api/devices`에서 복원되도록 변경. 기본 저장 경로는 `%APPDATA%\WonRemote\devices.json`이며 `WONREMOTE_API_STORE`로 테스트/개발 경로를 지정할 수 있다. 아직 상용 DB, 설치형 Windows 서비스, 실제 스트리밍/OS 입력 주입은 구현되지 않음.
- **16차 개정 (2026-06-11):** Agent heartbeat 및 offline 판정 추가. `/api/agent/heartbeat`가 등록 장비의 `lastSeenAt`과 online 상태를 갱신하고 JSON 저장소에 반영한다. `/api/devices`는 `lastSeenAt` 기준 stale 장비를 offline으로 표시하며, offline 임계값은 `WONREMOTE_AGENT_OFFLINE_MS`로 지정할 수 있다. CLI는 등록/재실행 후 heartbeat를 1회 전송하고 `npm run agent:watch`로 주기 전송할 수 있다. 아직 Windows 서비스 자동 실행, 네트워크 스트리밍, OS 입력 주입은 구현되지 않음.
- **17차 개정 (2026-06-11):** Viewer 입력 이벤트를 Agent까지 전달하는 메모리 기반 command queue 추가. `/api/sessions/:id/input` 호출 시 해당 세션의 deviceId에 명령을 적재하고, Agent CLI는 `/api/agent/commands` polling으로 pending 명령을 가져와 처리 로그를 남긴 뒤 큐를 비운다. 현재 명령 큐는 메모리 기반이며 실제 Win32 `SendInput` 주입, 스트리밍 화면 전송, 큐 영구 저장은 아직 구현되지 않음.
- **18차 개정 (2026-06-11):** PoC 수준의 타일 스트리밍 연결 검증. Rust `--mode stream`이 32x32 JPEG 타일 JSON line을 출력하고, Agent CLI가 `start-stream` 명령 수신 시 Rust stream을 시작해 `/api/sessions/:id/tiles`로 전송하며, API의 tiles POST/GET/clear 흐름과 Viewer Canvas 부분 렌더링을 확인함. 세션이 없을 때 Agent가 DXGI 캡처를 무조건 시작하던 동작은 제거함. 아직 Visual Ping `T_presented` 정밀 계측, 물리 입력 위치 정합성, 상용 전송망/보안/Windows 서비스는 검증 필요 상태임.
- **19차 개정 (2026-06-11):** Viewer Visual Ping의 마젠타 타일 감지, `requestAnimationFrame` 이후 픽셀 샘플링, `T_presented - T_start` 지연 계산, 150ms 초과 시 `set-sleep 100`으로 낮추는 보수적 스트림 주기 조정 로직을 테스트 가능한 `visualPing` 도메인 함수로 분리하고 App에 연결함. 이는 브라우저 Canvas 표시 시점 기반 PoC 계측이며, 실제 모니터 물리 표시/GPU fence, 기존 ZOOK 대조군, J1800/J1900 실장비 성능은 계속 검증 필요 상태임.
- **20차 개정 (2026-06-11):** Viewer 데스크톱 패키징 1차 골격을 Tauri v2로 확정. `aether-link-app/src-tauri`에 Windows WebView2 기반 데스크톱 쉘, NSIS 설치본 설정, 기본 권한 파일, 앱 아이콘, Cargo lockfile을 추가하고 `npm run desktop:dev`/`npm run desktop:build` 스크립트를 연결함. `package.json`, `tauri.conf.json`, `Cargo.toml` 버전 정합성 테스트를 추가해 자동 업데이트/설치본 버전 불일치 리스크를 차단함. 아직 Windows 트레이 아이콘, 시작 프로그램/서비스 등록, 설치 후 Agent/API 동시 구동 orchestration은 다음 구현 범위로 유지.
- **21차 개정 (2026-06-11):** Gemini의 트레이/시작프로그램/프로세스 orchestration 구현을 CodeX가 검수하고 배포 차단 리스크를 보수함. 설치 대상 PC의 시스템 Node 의존성을 제거하기 위해 `process.execPath`를 `dist-runtime/node.exe`로 복사해 Tauri 리소스에 포함하고, `desktop:build` 전 `cargo build --release`로 Rust PoC exe를 자동 준비하도록 변경. production API/Agent는 bundled Node로 실행하며 Agent는 기존 config 또는 명시 env가 있을 때만 `--watch`로 구동해 hidden prompt hang을 방지함. 트레이 startup toggle은 registry write 성공 후에만 UI checked 상태를 변경하도록 보정함. 아직 설치본 실행 후 실제 트레이 클릭/registry 값/프로세스 kill-on-exit의 물리 검증은 후속 Windows 런타임 검증 범위임.
- **22차 개정 (2026-06-11):** Gemini의 실제 설치본 물리 검증 결과를 CodeX가 재검토하고 테스트 격리 문제를 보수함. Agent와 API Server 동시 기동 경합은 `/api/health` 대기 루프로 완화하되, 해당 로직을 `waitForApiHealth` 함수와 단위 테스트로 분리해 회귀를 차단함. `/api/health`는 stateful route 초기화 전 200 응답해야 함을 API 테스트로 고정함. 기존 registry 테스트가 실제 `HKCU\...\Run\WonRemoteViewer` 값을 삭제할 수 있던 위험은 `Software\WonRemote\Tests\Run` 격리 키를 사용하는 테스트로 교체함.
- **23차 개정 (2026-06-12):** Agent 상시 백그라운드/숨겨진 아이콘 요구사항 반영분을 CodeX가 검수 및 보수함. Tauri `--agent` 모드에서 등록 전에는 최초 등록 창을 표시하고 등록 후에는 창을 숨긴 채 Agent tray를 유지하며, `WonRemoteAgent` HKCU Run 값은 현재 exe에 `--agent`를 붙여 등록되도록 명령 문자열 테스트를 추가함. `apiUrl`을 Agent config 타입에 명시하고, `dist-agent-pkg` 산출물은 Git 제외 처리함. `npm run desktop:build` 실패 원인은 이전에 실행 중이던 release `wonremote-viewer.exe`와 자식 `node.exe`의 파일 잠금으로 확인해 해당 빌드 산출물 프로세스 종료 후 재빌드 통과를 확인함. 단, `dist-agent-pkg`의 bat/Node payload는 tray가 없는 headless runtime 산출물이므로, 최종 사용자용 Agent tray 실행 경로는 Tauri exe의 `--agent` 모드로 유지해야 함.
